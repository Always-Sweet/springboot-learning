<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slm.mybatis.spring.mapper.AuthorMapper">

    <select id="findById" resultType="com.slm.mybatis.spring.entity.Author">
        select * from author where id = #{id}
    </select>

</mapper>